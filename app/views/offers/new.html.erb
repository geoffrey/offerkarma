<% if params[:debug] %>
  <% [1..4].each do |step| %>
    <%= render "offer_form_step#{step}", locals: { offer: @offer } %>
  <% end %>
<% else %>
  <%= render "offer_form_step#{@step}", locals: { offer: @offer } %>
<% end %>
