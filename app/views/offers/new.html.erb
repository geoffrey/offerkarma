<% if params[:debug] %>
  <% OffersController::NEW_OFFER_STEPS.each do |step| %>
    <%= render "offer_form_step_#{step}", locals: { offer: @offer } %>
  <% end %>
<% else %>
  <%= render "offer_form_step_#{@step}", locals: { offer: @offer } %>
<% end %>
