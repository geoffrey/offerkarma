<div class="text-center homepage-header mb-3 p-4 bg-dark">
	<div class="mb-3 text-white">
		<div class="mb-3 lead">
			Need feedback on a job offer?
		</div>
		<div class="mb-4">
			<a class="btn btn-success" href="/offers/new">Submit offer compensation</a>
		</div>
		<div class="small font-weight-light">
			<span class="text-dark bg-white p-1 font-weight-bold"><span class="text-danger">Comp</span>parator</span> anonymous community can let you know if your potential total compensation has room for negociations and coach you to get a better one.
		</div>
	</div>
</div>

<div class="container">
	<div class="lead mb-3 text-center">Offers waiting for some feedback</div>

	<ul class="featured-offer-list list-group">
		<% @offers.each do |offer| %>
			<a href="<%= offer_path offer.id %>" class="border rounded list-group-item-action mb-3">
				<%= render partial: "offer_list_item", locals: { offer: offer } %>
			</a>
		<% end %>
	</ul>
</div>