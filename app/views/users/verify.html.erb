<div class="text-center homepage-header p-4 bg-dark">
  <div class="text-white">
    <div class="lead">
      You are now verified <span class="fa fa-check"></span>
    </div>
  </div>
</div>

<div class="bg-light p-4">
  <div class="container bg-white center-box-max-width p-4 rounded">
    <div class="text-center">
      <div class="mb-3">
        <% if current_user.current_company %>
          <div class="lead mb-3">You are all set!</div>

          <div class="mb-3">
            We were able to verify that you are currently working for:
          </div>

          <div>
            <img width="80px" class="rounded" src="<%= current_user.current_company.domain.logo_url %>">
          </div>

          <div class="mb-3">
            <a href="<%= company_path current_user.current_company.uuid %>" class="btn btn-link text-danger">
              See offers from <%= current_user.current_company.name %>
            </a>
          </div>
        <% else %>
          <div class="font-weight-bold mb-3">
            Unfortunately, we did not detect any company from the email address you used to register with.
          </div>

          <div>
            Don't worry you can still use reffo. The only downside is that you won't be able to see some offers restricted to some companies.

            If you wish to be verified with a company, please sign up again using your work email this time.
          </div>
        <% end %>
      </div>

      <div>
        <% if current_user.offers&.none? %>
          <a href="<%= new_offer_path %>" class="btn btn-danger">Evaluate my first offer</a>
        <% else %>
          <a href="<%= account_path %>" class="btn btn-outline-danger">Manage my offers</a>
        <% end %>
        or <a href="<%= offers_path %>" class="btn btn-outline-danger">See our most recent offers</a>
      </div>
    </div>
  </div>
</div>
